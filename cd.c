#include "cd.h"

void MyCD(char *dir_input, int arg_count)
{
	/*
	    if (arg_count > 2) //arg[0] holds "cd" and args[1] holds users "path". If argcount > 2 then cd format is wrong
    {
        printf("Invalid use of cd\n");
        return;
    }

    if (arg_count == 2) //means the user has entered an argument
    {
        int num = chdir(dir_input);
        if (num == -1) //chdir returns a 0 on success and a -1 on failure
        {
            printf("Failed to go to path\n");
            return;
        }
    }


    if (arg_count == 1) //user entered no arguments. arg_count is 1 because args[0] holds "cd"
    {
        int num = chdir(); //give users path to chdir
        if (num == -1)              //chdir returns a 0 on success and a -1 on failure
        {
            printf("Failed to go to path\n");
            return;
        }
    }
	
	
	
	
	
	
	
	
	
	
	
	
	
    	if(arg_count != 1 && arg_count != 2)
	{
		printf("Invalid use of cd\n");
		return;
	}
	while (1)
   	{
        	if (COMMANDS[0] != NULL)
        	{
            		if(strcmp(COMMANDS[0], "cd"))
            		{
                	int chdir(const char *HOME);
            		}
            	else
                printf("Unable to open file. Exiting.\n");
                exit(1);
        	}
    }
	*/
}